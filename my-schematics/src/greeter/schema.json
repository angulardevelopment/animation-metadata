{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsGreeter",
  "title": "Greeter Options",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name to greet"
    }
  },
  "required": ["name"]
}
